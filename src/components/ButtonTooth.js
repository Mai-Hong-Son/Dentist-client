import React, { Component } from 'react';
import { View, Text } from 'react-native';

export default class ButtonTooth extends Component {
    render() {
        return (<View>
            <Text>dfs</Text>
        </View>);
    }
}